version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8u252

    steps:
      - test:
          command: ./gradlew clean test

      - check:
          command: ./gradlew check jacocoRootReport
